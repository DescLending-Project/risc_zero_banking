# TEE genereate score create
This create encapsulates code that:
1. Fetches needed defi_inputs
2. Validates defi_inputs
3. Calculates end credit_score
4. Returns data required by the CreditScore.sol contract

## TEST:
to execute test make sure first that the Contracts from solidity are deployed to the anvil node!
then run:
```bash
cargo test
```

